🚨【特殊格式延续 - 必须严格遵守】🚨
- 自动识别前文中的固定格式元素（如系统面板、状态显示、数值格式等）
- 若前文使用【xxx】格式显示状态，本章必须使用完全相同的格式
- 数值/时间格式必须与前文一致：
  * 如前文是"约70小时32分"，继续用"约XX小时XX分"
  * 如前文是"68:22:18"精确格式，继续用精确格式
  * 禁止在同一作品中混用不同格式
- 特殊格式内容只更新数值，不改变结构和措辞
- 禁止自行创造新的格式元素或扩展已有格式