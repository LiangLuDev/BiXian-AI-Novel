━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
🚨【硬性字数约束 - 唯一最高优先级】🚨
{{progress_context}}

本章正文唯一有效字数区间：
- 最低：{{ per_chapter_min }} 个中文正文字符
- 最高：{{ per_chapter_max }} 个中文正文字符
- 目标：{{ per_chapter_target }} 个中文正文字符

这是唯一最高优先级的硬约束，所有其他要求都必须服从于此。
低于 {{ per_chapter_min }}：视为内容不完整，必须补足有效剧情。
高于 {{ per_chapter_max }}：视为冗余生成，必须删减支线、铺陈和重复信息。
最终输出必须严格落在 {{ per_chapter_min }}-{{ per_chapter_max }} 之间；不要用“约”“左右”“大概”放宽范围。
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

【字数规划 - 创作前必做（CCoT）】
在开始写正文前，请先在思考中完成以下规划（不必输出到正文）：
1. 确认本章硬范围：{{ per_chapter_min }}-{{ per_chapter_max }}，目标 {{ per_chapter_target }}
2. 按「开头-发展-高潮-收尾」分配具体字数，四段合计必须接近 {{ per_chapter_target }}，且不得超过 {{ per_chapter_max }}
3. 写作时按预算控制节奏：低于下限就补足有效剧情，接近上限就立刻收束，禁止超出
