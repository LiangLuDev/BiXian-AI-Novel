━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
🚨【硬性字数约束 - 唯一最高优先级】🚨
{{progress_context}}

❌ 严格禁止超出字数范围：
   - 低于最低字数：视为内容不完整，必须补充有效剧情
   - 高于最高字数：视为冗余生成，必须删减无效内容
   - 允许区间为目标值 ±10%，超出视为失败生成

这是唯一最高优先级的硬约束，所有其他要求都必须服从于此。
在生成全过程中，必须持续监控字数，确保最终输出严格落在规定范围内。
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

【字数规划 - 创作前必做（CCoT）】
在开始写正文前，请先在思考中完成以下规划（不必输出到正文）：
1. 根据上方【本章目标】确认本章硬范围 min-max 字（必须写出具体数字，如 2610-3191）
2. 按「开头-发展-高潮-收尾」精确分配：开头 A 字、发展 B 字、高潮 C 字、收尾 D 字，且 A+B+C+D ≤ max。禁止使用"大约""左右""约"等模糊表述，必须给出具体数字（如 500、1500、800、700）
3. 写作时按此预算控制节奏，接近 max 时果断收尾，禁止超出